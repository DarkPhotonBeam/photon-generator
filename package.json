{
  "name": "photon-generator",
  "version": "0.0.1",
  "description": "",
  "main": "build/src/index.js",
  "types": "build/src/index.d.ts",
  "files": [
    "build/src"
  ],
  "license": "Apache-2.0",
  "keywords": [],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "gts lint",
    "clean": "gts clean",
    "compile": "tsc && rm -rf build/src/css && cp -r src/css build/src/css",
    "fix": "gts fix",
    "prepare": "npm run compile",
    "pretest": "npm run compile",
    "posttest": "npm run lint",
    "exe": "node build/src/index.js",
    "prepublish": "tsc && rm -rf build/src/css && cp -r src/css build/src/css"
  },
  "bin": "build/src/index.js",
  "devDependencies": {
    "@types/figlet": "^1.5.5",
    "@types/node": "^14.11.2",
    "gts": "^3.1.1",
    "typescript": "~4.7.0"
  },
  "dependencies": {
    "commander": "^10.0.0",
    "figlet": "^1.5.2",
    "showdown": "^2.1.0",
    "chalk": "^4"
  }
}
